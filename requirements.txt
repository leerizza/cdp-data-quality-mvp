# Engine
duckdb==1.3.2

# dbt builds the staging/unified models against the same DuckDB file.
dbt-core==1.12.3
dbt-duckdb==1.11.0

# Only used by notebooks/generate_dummy.ipynb to build the source data.
pandas==2.3.2
faker==37.8.0
